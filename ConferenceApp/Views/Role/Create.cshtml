@model ConferenceApp.Models.Role

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Role</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserId" class="control-label">Panelista</label>
                <select asp-for="UserId" class="form-control" asp-items="ViewBag.Panelists"></select>
            </div>
            <input type="hidden" asp-for="EventId" value=@ViewBag.EventId/>
            <input type="hidden" asp-for="Name" value="panelist"/>
            
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
