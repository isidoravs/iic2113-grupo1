@model IEnumerable<ConferenceApp.Models.ConferenceVersion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index: Versiones de Una Conferencia</h1>

<p>    
    <a asp-controller="Conference" asp-action="Details" asp-route-id="1">Volver a Conferencia</a>
    <br/>
    <hr/>
    <a asp-action="Create">Crear Nueva Version de Conferencia</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Numero de conferencia
            </th>
            <th>
                Inicio
            </th>
            <th>
                Fin
            </th>
            <th>
                Para organizador (Edit y Delete)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
